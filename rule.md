# 给 Cursor 的开发与系统指令

你好，Cursor。我是本项目的设计师。我们正在开发一款名为《秘术对决：禁忌魔典》(Arcane Duel: The Forbidden Grimoire) 的双人暗战卡牌博弈桌游。
我已经为你提供了该游戏完整的《游戏规则系统设计文档》。现在，请你扮演一位顶级的全栈工程师兼技术美术，按照以下要求为我构建该游戏的同屏对战（Pass & Play）数字原型。

## 1. 技术栈要求
为了快速验证核心玩法与状态机，我们第一版原型使用 **Python + Streamlit** 构建。
* 使用 `st.session_state` 严格管理所有游戏状态（手牌、牌库、当前阶段、破法者标记、分数等）。
* 游戏必须支持同屏的 Pass & Play 模式。考虑到隐私，在“汲取”、“突袭”和“咏唱”阶段，必须设计一个“防偷看遮罩”：当前回合玩家点击确认身份后，才展示其真实手牌和操作按钮。
* 代码必须模块化：将游戏逻辑（发牌、计分计算、胜负判定）与 UI 渲染（Streamlit 组件）分离。

## 2. 界面布局与交互逻辑 (基于 Streamlit)
请充分利用 Streamlit 的布局能力（`st.columns`, `st.sidebar`, `st.expander` 等）实现以下区域：
* **侧边栏 (Sidebar - 公共战况区)：** 显示当前牌库剩余数量、双方分数、双方持有的【破法者标记】数量、弃牌堆展示。
* **顶部区 (状态栏)：** 高亮显示当前是谁的回合，以及当前处于四大阶段中的哪一个阶段。
* **主战区 (Main Area)：**
    * 当处于【突袭阶段】时，渲染双盲拼点的操作交互（用 `st.selectbox` 或按钮实现）。
    * 当处于【咏唱阶段】时，提供复选框 (`st.multiselect`) 供玩家选择手牌，并提供按钮识别计分组合或进行【黑暗献祭】。
* **底部区 (手牌区)：** 玩家的手牌列表。请按照卡牌基础分（A到F）进行排序显示，方便玩家查看。

## 3. 视觉美学降维映射 (Dark Academia 风格)
虽然 Streamlit 的前端定制能力有限，但请你尽可能通过 Streamlit 的 Markdown、HTML/CSS 注入 (`st.markdown(..., unsafe_allow_html=True)`) 来映射我们游戏的“暗黑学院派”风格：
* **色彩基调：** 界面背景请尝试注入深胡桃木色或暗红色调。
* **卡牌视觉：** 不要只用纯文本显示 A、B、C。请使用 HTML/CSS 绘制具有塔罗牌长宽比的卡牌样式。例如：
    * **A (圣物)：** 红色底色，带有金色的文字和高亮边框。
    * **F (杂鱼)：** 灰褐色底色。
* **光影与提示：** 善用 `st.error`（红色，用于魔力过载/错误提示）、`st.warning`（黄色，用于突袭阶段警告）、`st.success`（绿色，用于加分/胜利）来引导玩家情绪。

## 4. 开发步骤拆解 (Step-by-Step)
请不要一次性生成所有代码。请按照以下步骤，每次只生成一步的代码供我运行测试，测试通过后再进行下一步：
* **步骤一：** 初始化 `st.session_state`、洗牌逻辑、发牌逻辑、基础侧边栏 UI，以及回合轮转和“防偷看遮罩”系统。
* **步骤二：** 完善【1.汲取阶段】（处理“瞬”的透支）和【4.整理阶段】（手牌上限强制弃牌逻辑）。
* **步骤三：** 攻克最复杂的【2.突袭阶段】。实现强迫应战、胜负比较（F弑神机制）、【圣物威压】10分奖励，以及极其重要的【有限拾荒】逻辑。
* **步骤四：** 实现【3.咏唱阶段】。编写组合计分识别算法（包括“瞬时回响”连击加成），以及【黑暗献祭】防卡手逻辑。
* **步骤五：** 植入终局大招——当牌库为 0 时触发的【魔力对撞】终焉回合逻辑（包括底池翻倍累加等）。

请确认你已完全阅读并理解《游戏规则系统设计文档》及上述开发指令。如果理解，请直接开始**步骤一**的代码编写！