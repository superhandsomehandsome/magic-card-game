services:
  - type: web
    name: magic-card
    runtime: python
    buildCommand: echo "pre-built frontend included"
    startCommand: python serve_frontend.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
