services:
  - type: web
    name: magic-card
    runtime: python
    buildCommand: cd frontend && npm install && npm run build
    startCommand: python serve_frontend.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
