{
  "name": "magic-card-game",
  "version": "6.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npx serve frontend/dist -s -l ${PORT:-3000}"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
